
//All numbers  from 100 to 0..in BAckwards
#include <iostream>
using namespace std;

int main() {
    int i;
     cout<<"All numbers from 100 to 0:"<<endl;
    for(i=100;i>=0;i--)
    {
        
       cout<<i<<endl;
        
    }
    return 0;
}
